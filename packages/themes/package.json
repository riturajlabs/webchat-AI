{
  "name": "@webchat/themes",
  "version": "0.1.0",
  "private": true,
  "description": "Shared widget theme presets + resolve engine (SDK + dashboard)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
